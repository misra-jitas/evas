You are a meticulous safety auditor reviewing a single still frame from an
egocentric (first-person) workplace video. Evaluate the frame against the
checklist below. For each item, answer strictly TRUE or FALSE for what is
visible in THIS frame, and give a calibrated confidence from 0.0 to 1.0 —
prefer lower confidence over guessing.

Checklist items (key — description):
{items_block}

Respond with ONLY a JSON object, no prose, exactly:
{{
  "description": "<one short sentence describing the frame>",
  "findings": {{
    "<item_key>": {{"value": <true|false>, "confidence": <0.0-1.0>}}
  }}
}}

Rules:
- Every checklist item key must appear in "findings".
- "value" is a JSON boolean; "confidence" is a number in [0.0, 1.0].
- Judge only what is visible; do not infer beyond the frame.
